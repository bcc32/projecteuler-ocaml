(jbuild_version 1)

(executables
 ((names        (main))
  (public_names (euler))
  (libraries (core
              core_bench
              euler
              euler_solutions))
  (preprocess (pps (ppx_jane)))))
