(library
 ((name        euler)
  (public_name euler)
  (synopsis "a collection of utilities for solving ProjectEuler math problems")
  (libraries (core
              bignum))
  (inline_tests)
  (preprocess (pps (ppx_jane)))))
